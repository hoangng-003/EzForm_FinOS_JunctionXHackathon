const User = require("./User");
const { Form } = require("./Form");
const GuessData = require("./GuessData");
const TempData = require("./TempData");

module.exports = { User, Form, GuessData, TempData };
